export interface <%= classify(name) %> {
  <% for(let e in entity.fields) { %>
    <%= entity.fields[e].name %><% if(entity.fields[e].required) { %>?<% } %>: <%= entity.fields[e].input_type.name %>;
  <% } %>
}
